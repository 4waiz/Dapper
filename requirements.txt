# Core benchmark (required)
numpy>=1.24.0
pandas>=2.0.0
pyyaml>=6.0
matplotlib>=3.7.0
pytest>=7.4.0
pyarrow>=12.0.0          # parquet cache for the real-detector study

# Optional: FastAPI demo service in legacy/edge_server.py and app.py
fastapi>=0.100.0
uvicorn>=0.22.0
pydantic>=2.0.0
requests>=2.31.0

# Optional: secondary real-detector validation (needs a CUDA GPU to be quick).
# Install separately; the main benchmark does not import these.
#   pip install torch --index-url https://download.pytorch.org/whl/cu128
#   pip install ultralytics>=8.3.0
